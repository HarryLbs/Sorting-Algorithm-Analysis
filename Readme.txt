Program creates an array of size inputted from user either In-order, reverse order, almost order, or random order.
The array will then be sorted with either a insertion, selection, quick, merge, heap, or radix sort.
The program will then output the number of comparrisons and movements it took to complete the sort.